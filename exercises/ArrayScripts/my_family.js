let family = ["LaKeisha", "Erie", "Chunks","Erie'onna","Bill"];

// let index = 0;
//  while (index < family.length) {
    // console.log(family[index]);
    //  let familyMemeberName = family[index];
    //  console.log(family.length);
    // index++;
//  }
for (let index = 0; index < family.length; index++) {
   let familyMemeberName = family[index];
   console.log(familyMemeberName);
  }
 
